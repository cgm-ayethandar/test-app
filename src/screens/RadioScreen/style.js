import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    marginTop: 20,
  },
  title: {
    fontSize: 18,
    fontWeight: "500",
    paddingLeft: 10,
  },
});

export default styles;
