import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
      alignItems: "center",
      borderBottomWidth: 1,
      borderColor: "#DCDCDC",
      flexDirection: "row",
      paddingHorizontal: 5,
      paddingVertical: 8,
      width: "95%",
    },
    textContainer: {
        marginLeft: 25,
    },
    label: {
        fontSize: 16,
        fontWeight: "400",
        marginBottom: 3,
    },
  });

export default styles;