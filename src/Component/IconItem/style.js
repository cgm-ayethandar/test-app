import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
      width: "95%",
      flexDirection: "row",
      paddingVertical: 8,
      paddingHorizontal: 5,
      alignItems: "center",
      borderBottomWidth: 1,
      borderColor: "#DCDCDC",
    },
    textContainer: {
        marginLeft: 25,
    },
    label: {
        fontSize: 16,
        fontWeight: "400",
        marginBottom: 3,
    },
  });

export default styles;